{
  "license": "UNLICENSED",
  "scripts": {
    "check:lint:md": "find . -type f  -name node_modules -prune -name '*.md' -exec npx markdownlint --config .markdownlintrc.json {} \\;"
  },
  "dependencies": {
    "markdownlint": "^0.23.1"
  }
}
